{
  "name": "@counter-bridge/consumer",
  "version": "0.1.0",
  "description": "Standalone consumer service for CounterBridge",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@counter-bridge/core": "0.1.0",
    "@counter-bridge/provider-mongo": "0.1.0",
    "@counter-bridge/types": "0.1.0",
    "mongoose": "^8.0.0",
    "yaml": "^2.7.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
