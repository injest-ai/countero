{
  "name": "@counter-bridge/sdk",
  "version": "0.1.0",
  "description": "Client SDK for pushing counter events to CounterBridge",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "keywords": ["counter", "redis", "sdk", "client"],
  "license": "MIT",
  "dependencies": {
    "@counter-bridge/types": "0.1.0",
    "ioredis": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
