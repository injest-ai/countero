{
  "name": "@counter-bridge/types",
  "version": "0.1.0",
  "description": "Shared TypeScript interfaces for CounterBridge",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "keywords": ["counter", "redis", "sync", "types"],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
