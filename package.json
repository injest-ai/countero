{
  "name": "counter-bridge-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@11.8.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "test": "turbo run test",
    "test:watch": "vitest",
    "dev": "turbo run build --watch"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "turbo": "^2.8.3",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  }
}
